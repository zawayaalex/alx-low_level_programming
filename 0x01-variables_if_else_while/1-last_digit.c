#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: C code that prints with put function
 *
 * Return: 0 (success)
*/


int main(void)
{
  int n, digit;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	digit = n % 10;
	if (digit > 5)
	  printf("")
	    elde if (digit == 0)
	    printf("")
	    else if (digit < 6 && digit !=0)
	      printf("")
	return (0);
}
