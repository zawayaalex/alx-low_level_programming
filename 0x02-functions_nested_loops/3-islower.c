#include "main.h"

/**
 * _islower - is lower case
 * @c: is the letter to be checked
 * Return: 1 if yes and 0 if no
 */

int _islower(int c)
{
if (c > 96 && c < 123)
{
return (1);
}
else
{
return (0);
}
}
