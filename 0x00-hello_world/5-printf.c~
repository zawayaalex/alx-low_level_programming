#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: C code that prints with printf function
 *
 * Return: 0 (success)
*/

int main(void)
{
  printf9("with proper grammar, but the outcome is a piece of art,\n ");
return (0);
}
